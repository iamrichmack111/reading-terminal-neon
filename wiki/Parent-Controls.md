# Parent Controls

Set the parent PIN when launching the application:

```bash
READING_PARENT_PIN=4826 ./reading_terminal_neon.py
```

The parent command can be opened from supported prompts with:

```text
:parent
```

Parent controls are intended for progress management and administrative actions without making the child's normal lesson flow more complicated.
